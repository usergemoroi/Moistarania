# Релизные APK файлы

Эта папка содержит собранные APK файлы приложения.

## v4fer.apk ✅

**АКТУАЛЬНАЯ ВЕРСИЯ** - исправленная версия APK с корректной подписью:
- Обновлены versionCode/versionName для новой сборки
- Подписан с использованием debug keystore (androiddebugkey)
- Все функции приложения сохранены в полном объеме
- Включает assets/so2-external-main.zip с native overlay

## v3fg.apk ✅

**РЕКОМЕНДУЕМАЯ ВЕРСИЯ** - исправленная версия APK с корректной подписью:
- **Исправлена ошибка "пакет недействителен или поврежден"** - APK теперь должным образом подписан
- Содержит все необходимые файлы подписи в META-INF (MANIFEST.MF, ANDROIDD.SF, ANDROIDD.RSA)
- Подписан с использованием debug keystore (androiddebugkey)
- Все функции приложения сохранены в полном объеме
- Включает assets/so2-external-main.zip с native overlay

### Установка

```bash
adb install -r v3fg.apk
```

### Примечание

APK должен устанавливаться без ошибок "пакет поврежден". Если проблема все еще возникает, убедитесь что:
- Вы используете актуальную версию Android (API 21+)
- У вас отключена проверка подписи при обновлении (если устанавливается поверх старой версии с другой подписью)

---

## v2gege.apk ⚠️

**НЕ РЕКОМЕНДУЕТСЯ** - оригинальная версия БЕЗ подписи:
- Вызывает ошибку "пакет недействителен или поврежден" при установке
- Не содержит файлов подписи в META-INF
- Рекомендуется использовать v3fg.apk вместо этой версии

---

## v1test.apk

Упрощённая версия приложения:
- Убран весь Kotlin Material Design GUI
- MainActivity автоматически запускает InjectorService при открытии
- Прозрачный минималистичный интерфейс (без кнопок и UI элементов)
- ImGui overlay из so2-external-main.zip остаётся без изменений
- Все функции запуска overlay сохранены (root check, native builder, injector service)
- Все разрешения в AndroidManifest.xml сохранены

### Установка

```bash
adb install -r v1test.apk
```

### Использование

1. Запустите приложение - оно автоматически:
   - Проверит root доступ
   - Проверит разрешения
   - Соберёт native overlay
   - Запустит InjectorService
   - Запустит Standoff 2 (если установлена)
   - Закроется после успешного запуска overlay

2. Взаимодействие с overlay происходит через ImGui интерфейс
